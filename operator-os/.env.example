# Override the base directory for agent-os filesystem
# Defaults to ~/agent-os (i.e. $HOME/agent-os)
# AGENT_OS_HOME=/custom/path/to/agent-os

# Optional: change the dev port (also pass --port 3737 to npm run dev)
# PORT=3737
